fullnameOverride: "rabbitmq-server"

auth:
  username: guest
  password: guest

podAnnotations:
  prometheus.io/scrape: "true"

metrics:
  enabled: true

singleNode:
  persistence:
    enabled: false
