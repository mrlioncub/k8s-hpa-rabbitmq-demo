server:
  scrape:
    enabled: true
    config:
      scrape_configs:
      - job_name: rabbitmq
        static_configs:
        - targets:
          - rabbitmq-server:15692
  persistentVolume:
      enabled: false
