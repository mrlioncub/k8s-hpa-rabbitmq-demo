# Default values for rabbitmq-agent.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: mrlioncub/rabbitmq-agent
  tag: latest
  pullPolicy: IfNotPresent
